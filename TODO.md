# UI Test Backlog

Backlog for the UI e2e phase of the Playwright framework. Agreed 2026-04-24, before concluding UI and moving on to API tests.

## Ground rule
Do not mark an entry as [x] until a test run has confirmed the code passes. "Written" is not "done." If a TC fails, update the entry with the failure and next step rather than ticking it. Learned on 2026-04-24 after otp-login TC12 was prematurely ticked and then had to be reverted.

## Goal
Portfolio-style example framework demonstrating core Playwright concepts — something that can be consulted as a reference in future sessions and shown as a skills portfolio.

## Pending new specs

Each spec below targets a Playwright concept not yet demonstrated. Recommended order: top-down.

- [x] **file-upload.spec.ts** (`/upload`) — `setInputFiles()`, size BVA (499/500/501 KB), recovery after size error and after native required validation. 8 TCs. Single-file only (input has `multiple=false`); no type filter (`accept=""`); drag-and-drop skipped (no custom drop zone). Fixture files generated at runtime into `test-artifacts/upload/` via `Buffer.alloc(N * 1024)`. Added 2026-04-24.
- [x] **file-download.spec.ts** (`/download`) — `Promise.all` + `page.waitForEvent('download')`, `download.suggestedFilename()`, `download.saveAs()` to `testInfo.outputDir` (per-(spec, test, project) isolation, kept-on-failure), text/JSON/PNG/JPEG content + magic-byte assertions, `download.url()` + `download.failure()` page-level invariants looped across all stable files, `page.on('download')` listener pattern. 7 TCs. Targets only the 5 stable demo files (`some-file.txt`, `some-file.json`, `wdio.png`, `cdct.jpg`, `xpath-css.png`) — timestamped user-uploaded files on the page rotate and are excluded as non-deterministic. No global-setup/teardown wiring; Playwright's per-test `outputDir` replaces explicit cleanup. Added 2026-04-27.
- [x] **http-auth.spec.ts** (`/basic-auth` + `/digest-auth` + `/download-secure`) — `test.use({ httpCredentials })` for context-level Basic auth, scoped per describe block (no-creds / wrong-creds / valid-creds). Negative paths assert `response.status()` from `page.goto`; valid paths assert page H1, success alert, protected file list rendering, protected download (creds persist on the sub-resource fetch, not just navigation), and single-context persistence across all three protected URLs sharing the "Restricted Area" realm. 7 TCs. `/digest-auth` is mislabeled — server actually uses Basic auth; documented as a CLAUDE.md gotcha. `admin/admin` reaches all three endpoints. `triggerDownload` extracted to `helpers/downloads.ts` and shared with `FileDownloadPage` to avoid duplication. Downloads saved to `testInfo.outputDir`. Added 2026-04-27.
- [x] **iframe.spec.ts** (`/iframe`) — `page.frameLocator('#email-subscribe')` to enter a same-origin iframe, frame-scoped locators for the embedded Bootstrap `needs-validation` subscribe form (input/button/heading/`.invalid-feedback`/`#success-message`). Covers smoke access (frame heading), EP negative paths (empty + malformed email both surface `.invalid-feedback`), happy path (valid email replaces the form's parent `#subscribe` div with `#success-message`), and a state-transition recovery flow inside the frame. 5 TCs. Targets only the email-subscribe iframe — YouTube embed is blocked by Chromium and the "TinyMCE Editor" section is not actually an iframe; both documented as a CLAUDE.md gotcha. Added 2026-04-27.
- [x] **multiple-windows.spec.ts** (`/windows`) — canonical `Promise.all([context.waitForEvent('page'), link.click()])` pattern wrapped in a POM `openNewWindow()` helper, `newPage.waitForLoadState('domcontentloaded')`, `newPage.context() === page.context()` to assert shared `BrowserContext`, `context.pages()` lifecycle assertions (1 → 2 → 1), `page.bringToFront()` to return focus, and an `openMultipleWindows(N)` helper that opens popups sequentially (parallel clicks would race the `page` event listener). 6 TCs. Opener `/windows` is one H1 + a bare `<a href="/windows/new" target="_blank">Click Here</a>` (no id/class/JS); popup `/windows/new` is same-origin and contains only an H1. No EP/BVA/decision-table TCs — no input surface; state transition (open → close → re-open) covered as TC06. Added 2026-05-11.
- [x] **hovers-tooltips.spec.ts** (`/hovers` + `/tooltips` combined in one spec) — `locator.hover()` against CSS-only `.figcaption` reveal, data-driven loops (per-user / per-button) for parallel coverage, mouse-leave via `page.mouse.move(0, 0)`, `toBeAttached/not.toBeAttached` against Bootstrap's dynamic tooltip DOM, Bootstrap's `'hover focus'` trigger semantics including click→focus retention and click-elsewhere blur. 17 TCs across two `test.describe` blocks (Hovers TC01–TC08, Tooltips TC09–TC17). Gotchas surfaced during development: (1) Bootstrap 5 strips the `title` attribute on first show, so titles must be cached *before* any hover (caught in TC17 — fixed by reading `getAttribute('title')` up front). (2) A third-party consent-management UI injects ~85 `<button role="tooltip" class="fc-help-tip">` nodes asynchronously, breaking a broad `[role="tooltip"]` selector (caught in TC15 — fixed by narrowing the POM's `getTooltip()` to `div.tooltip[role="tooltip"]`). (3) Internal eager `toBeAttached` assertions inside POM action methods trip strict mode during click-driven cross-button transitions where two tooltips are momentarily attached; callers' own auto-retrying assertions are the right place to wait. (4) Hover-reveal + click races `scrollIntoViewIfNeeded`: hovering changes layout (caption display:none → block), a later click's scroll shifts the page, mouse loses the `:hover` figure, caption hides, click times out — fixed in `hoverUser()` by `scrollIntoViewIfNeeded()` *before* `hover()`. All four documented as CLAUDE.md gotchas. Added 2026-05-11.
- [x] **key-presses.spec.ts** (`/key-presses`) — `page.keyboard.press()` for named/single keys, `page.keyboard.type()` for sequences, modifier chords via `'Shift+a'`, data-driven loops with explicit `tc` numbers per row for sequential generated titles. Page handler is `$(document).keydown` writing `"You entered: <keyboardMap[event.which]>"` to `#result`; `event.which` is the legacy jQuery keyCode (physical key) so letters always render uppercase regardless of Shift state. 22 TCs: smoke (TC01), alphanumeric BVA `a`/`z`/`0`/`9` (TC02-05), specials Enter/Tab/Escape/Space/Backspace (TC06-10), arrows (TC11-14), modifiers pressed alone Shift/Control/Alt/Meta (TC15-18), Shift+a combo demonstrating last-keydown-wins (TC19), state transition `a`→`b` with `#target.value` accumulation (TC20), typed alphanumeric `abc123` (TC21), typed unshifted specials `.,/` → "SLASH" (TC22). Gotchas surfaced and documented in CLAUDE.md: (1) `#target` lives inside a hidden `<form method="get">` — Enter while focused submits and reloads the page to `/key-presses?`, so auto-focus removed from `beforeEach` and added only to TC19-TC22 which read `#target.value`. (2) Playwright's `press('Shift+a')` chord does NOT compose uppercase characters in the input — TC19 asserts lowercase `'a'`, with comment explaining the distinction. Function keys deliberately out of scope per TODO ("event-capture display only", "no retrofits to other specs"); shifted symbols (`!`, `@`, etc.) avoided in TC22 because `event.which` is keycode not character, making the result text non-obvious. Added 2026-05-11.

## Pending additions to existing specs

- [~] **otp-login.spec.ts TC12** — attempted 2026-04-24, reverted the same day. Test assumed the OTP was global; it is actually hard-coded to the canonical `practice@expandtesting.com` / `214365` pair, so no other email can reach `/secure`. Documented as a CLAUDE.md gotcha instead ("OTP login has one hard-coded email/code pair").

## Decision-table revisit — complete

Reviewed `form-validation`, `register`, `login`, `otp-login`. **No new TCs added.** The only candidate (otp-login TC12 above) turned out to be unreachable because of the site's hard-coded OTP pair. All four specs already exercise every distinct outcome reachable on the practice site.

## Out of scope (confirmed, not planned)

- Dynamic/wait pages: `/dynamic-loading`, `/dynamic-controls`, `/slow`, `/shifting-content`, `/disappearing-elements`, `/infinite-scroll`, `/scrollbars`
- DOM-robustness pages: `/shadowdom`, `/challenging-dom`, `/large`, `/dynamic-id`
- Visual snapshots (`toHaveScreenshot`), accessibility scans, network interception, storage/cookie inspection
- Integration between UI and API phases (phases stay independent)

## Decisions locked

- Hovers + tooltips → one combined spec, not two
- Keyboard → event-capture only, no Tab-focus retrofits in other specs
- HTTP auth → `newContext({ httpCredentials })` only (no URL-embedded credentials variant)
- File download → demonstrate `saveAs()` to a project path with cleanup

# API Phase

Teaching plan agreed 2026-05-13; see memory `project_api_phase_plan.md` for the full plan. Same ground rule applies: do not mark a lesson `[x]` until the corresponding code is written **and** test runs pass.

## Lesson progress

- [x] **L0 — HTTP & API fundamentals** — walkthrough only, no code. Covered verbs (GET/POST/PUT/PATCH/DELETE), idempotency, status code families (200/201/204, 400/401/403/404/409/422, 500), request anatomy (path/query/body/headers), JSON content-type, auth patterns (basic / cookie session / bearer-or-token), and the UI-vs-API mindset shift (no auto-retry assertions). Added 2026-05-13.
- [x] **L1 — `request` fixture + service-wrapper pattern** — `tests/api/health/health.spec.ts`, 5 TCs across `chromium`/`firefox`/`webkit`-free `api` project. TC01 happy path asserts HTTP status + `success`/`status`/`message` body keys; TC02–TC05 are a parameterised loop over `POST`/`PUT`/`PATCH`/`DELETE` using `request.fetch(url, { method })` to keep the endpoint in the service. `HealthService.ts` introduces the service-wrapper pattern (API equivalent of POM) with a private `endpoint` field and two methods (`check()`, `sendWithMethod()`). Decisions locked: baseURL stays as origin only (`https://practice.expandtesting.com`) — services carry the full `/notes/api/...` path so each is self-documenting against the Swagger; services folder lives at `services/` (sibling to `pages/`). Findings: Notes API returns **404 with HTML body** (not 405 + JSON) for wrong-verb-on-valid-path — implementation reality vs REST best practice; status-only assertion locks the contract without coupling to the framework's HTML error page. Leftover scaffolding `tests/api/users/users.spec.ts` deleted; `helpers/api.ts` and `helpers/data.ts` flagged as currently unused but kept (data helpers reused from L3, api helpers reassessed in L5). Added 2026-05-13.
- [x] **L2 — Swagger inspection + `tests/api/CLAUDE.md` + spec backlog** — inspected `https://practice.expandtesting.com/notes/api/swagger.json` (17 endpoints across Health/Users/Notes). Drafted `tests/api/CLAUDE.md` mirroring `tests/ui/CLAUDE.md` (conventions, service design rules, API-specific assertion preferences, 5 known gotchas, what-not-to-do). Spec backlog locked below. Findings: (1) request bodies are `application/x-www-form-urlencoded` per Swagger — use Playwright's `{ form: { ... } }`, not `{ data: { ... } }`; (2) auth header is `x-auth-token` (lowercase per spec); (3) `PATCH /notes/{id}` is a completion-only toggle, not a partial update — full edits go via `PUT`; (4) `DELETE` is the verb for `/users/logout` and `/users/delete-account` (REST-resource framing — defensible, not a bug); (5) `/users/forgot-password`, `/users/verify-reset-password-token`, `/users/reset-password` need email-inbox access to complete the token flow — declared out of scope. Added 2026-05-14.
- [x] **L3 — User registration (POST, BVA, decision table)** — `tests/api/users/register.spec.ts`, 19 TCs all green. `UserService` introduced with `register()` method (form-encoded body, inline-literal pattern to sidestep TS index-signature on the named payload type). Coverage: TC01 happy path with full body contract; TC02–TC07 password BVA (5/6/7/29/30/31, parameterised loop); TC08–TC13 name BVA (3/4/5/29/30/31, parameterised loop); TC14 invalid email EP; TC15–TC17 empty-field handling; TC18 duplicate-email 409 (self-contained — registers user A, then re-registers with same email); TC19 multi-field-invalid validator priority. Findings: (1) `randomString()` had a silent-truncation bug — `Math.random().toString(36)` returns ~11–13 chars max, so `randomString(50)` returned ~13 chars and made the 30-char password upper bound *appear* unenforced. Fixed with a while-loop that concatenates multiple `toString(36)` outputs until `length`. (2) `data.id` is a 24-char MongoDB ObjectId string everywhere, despite the Swagger example showing `"id": 6` — gotcha A in `tests/api/CLAUDE.md` revised to lock this. (3) Validator priority on register is `name → email → password` (first-failure-wins, never aggregated) — empty fields fall into the same length/format validators as out-of-range values, no separate "empty" path. (4) Notes API correctly enforces the 30-char password max (verified up to 5000 chars via curl) — the "API bug" was our helper. `helpers/api.ts` deleted (genuine dead code after committing to service-wrapper pattern); `helpers/data.ts` `randomInt` kept (no concrete use yet, reassess in L7). Added 2026-05-15.
- [x] **L4 — Login & token auth (authed request context fixture)** — Three deliverables landed: `tests/api/users/login.spec.ts` (7 TCs), `fixtures/index.ts` `authedRequest` worker-scoped fixture, and `tests/api/users/profile.spec.ts` (TC01 only — promoted from the throwaway smoke spec; remaining TCs land in L6). `UserService` extended with `login()` and `getProfile()` methods. Login coverage: decision table over (valid/invalid/empty email) × (valid/invalid/empty password), token format `/^[a-f0-9]{64}$/`, state transition anon→auth. Findings: (1) `POST /users/login` returns **identical 401s** for `wrong-password` and `non-existent-email` — security property preventing account enumeration; the two TCs share the exact assertion. Documented in `tests/api/CLAUDE.md`. (2) Worker-scoped fixtures use the tuple form `[setupFn, { scope: "worker" }]` and receive an empty destructure `{}` (no test-scoped fixtures available — `playwright`/`request`/`page` are unavailable; use the named `import { request } from "@playwright/test"` for `newContext`). (3) `request.newContext({ baseURL })` does NOT inherit the project's `baseURL` — must be passed explicitly to each new context. (4) `randomEmail()` strengthened with a 6-char base36 suffix to prevent same-millisecond worker collisions on parallel fixture setups. (5) **Multi-project leak**: API specs were running 4× because browser projects inherited the global `testDir: "./tests"` and scanned `tests/api/` too. Fixed by adding explicit `testDir: "./tests/ui"` to each browser project — now a `tests/api/CLAUDE.md` convention ("every project declares an explicit testDir"). (6) Diagnostic story: a duplicate `await authed.delete(...)` call in the teardown made cleanup appear broken (second call returned 401 — account already gone from first call). Surfaced via `console.log` instrumentation and a post-delete login probe; both confirmed cleanup *is* working. Verification removed after one-shot proof — fixture should not self-validate every run (no enforced assertion in `console.log`; cost-vs-benefit doesn't justify it). Throwaway `auth-smoke.spec.ts` promoted to `profile.spec.ts` TC01 to avoid duplicating future L6 coverage. 32 TCs green across the api project (health 5 + register 19 + login 7 + profile 1). Added 2026-05-15.
- [ ] L5 — Notes CRUD + state transitions
- [ ] L6 — Negative testing & error contracts
- [ ] L7 — Test data strategy & parallelism
- [ ] L8 — Schema validation with zod (optional)

## Spec backlog

Locked 2026-05-14 in Lesson 2. Each entry is a future TODO entry — happy-path and negative-path TCs are merged into one spec per resource/feature (same convention as the UI phase). Order matches the lesson order.

- [x] **users/register.spec.ts** (`POST /users/register`) — 19 TCs all green. EP on each field with field-specific 400 message assertions, BVA on password length (5/6/7, 29/30/31) and name length (3/4/5, 29/30/31) per discovered bounds (password 6–30, name 4–30), decision table for (name × email × password) validity classes (validator priority: name → email → password, first-failure-wins), 409 + exact-message on duplicate email. `name` field accepts special characters and digits — no character-set EP class. Body type: `form` (Notes API uses `application/x-www-form-urlencoded`, not JSON). Spec at `tests/api/users/register.spec.ts`, service at `services/UserService.ts`. Added 2026-05-15.
- [x] **users/login.spec.ts** (`POST /users/login`) — 7 TCs all green. Decision table (valid/invalid/empty email) × (valid/invalid/empty password) covering happy path with full body contract + token format `/^[a-f0-9]{64}$/`, wrong-password 401, non-existent-email 401 (intentionally same assertion — documents security property), invalid email format 400, empty email 400, empty password 400, both-empty 400 (email validator wins). Setup discipline: TCs that require an existing user (TC01 happy, TC02 wrong-password) register inline; TCs 03–07 skip setup because the response is determined before any DB lookup. Spec at `tests/api/users/login.spec.ts`. Added 2026-05-15.
- [x] **`authedRequest` fixture** (`fixtures/index.ts` extension) — worker-scoped fixture using the tuple form `[setupFn, { scope: "worker" }]`. Setup: creates an unauthenticated `setupContext`, registers a fresh user (random email with millisecond + 6-char suffix to avoid collisions), logs in to capture the 64-char hex token, disposes the setupContext, then creates a second `APIRequestContext` with `extraHTTPHeaders: { 'x-auth-token': <token> }` which is handed to the test via `use(authed)`. Teardown: `DELETE /users/delete-account` then dispose. Two contexts because `extraHTTPHeaders` is set at context-creation time and cannot be added retroactively. Setup uses the module-level `import { request } from "@playwright/test"` since worker fixtures cannot receive test-scoped deps like `playwright`/`request`/`page`. Each context explicitly sets `baseURL: BASE_URL` because `newContext()` does NOT inherit the project's baseURL. Cypress analogue: `cy.session`. Verified working in L4 via the (now-removed) auth-smoke spec — cleanup confirmed by post-delete 401 on re-login. Fixture at `fixtures/index.ts`. Added 2026-05-15.
- [ ] **notes/notes.spec.ts** (full CRUD + state transitions + negatives) — `POST /notes` → `GET /notes` (list) → `GET /notes/{id}` → `PUT /notes/{id}` (full replace) → `PATCH /notes/{id}` (completion toggle) → `DELETE /notes/{id}`. State transitions: created → updated → completed → deleted. Category enum (Home/Work/Personal) validation. Negative paths merged in: missing token (401), other-user's note (403), nonexistent id (404), invalid category (400), missing required fields (400). `afterEach` cleans up. ~16–20 TCs. Maps to L5 (CRUD) + L6 (negatives folded in).
- [ ] **users/profile.spec.ts** (`GET /users/profile`, `PATCH /users/profile`) — TC01 in place (GET happy path, promoted from the L4 auth-smoke spec to avoid duplication). Pending L6: TC02+ for full GET body contract (name, email, message lock), PATCH happy path, EP on `name`/`phone`/`company`, negative paths (missing token → 401, malformed token → 401). Primary vehicle for negative-testing technique practice that doesn't fit notes. ~5–6 TCs total. Maps to L6.
- [ ] **users/change-password.spec.ts** (`POST /users/change-password`) — Multi-stage flow demonstration: change password → login with new password succeeds → login with old password fails (401). Negative paths: wrong current password, missing token, weak new password. ~4–5 TCs. Maps to L7.
- [ ] **notes/notes-schema.spec.ts** *(optional)* — `zod` schema validation over one `POST /notes` response. Demonstration only; if adopted, decision recorded in `tests/api/CLAUDE.md`. ~2–3 TCs. Maps to L8.

## Out of scope (API phase)

- **`/users/forgot-password`, `/users/verify-reset-password-token`, `/users/reset-password`** — full flow requires retrieving a reset token from an email inbox. The practice site does not expose the inbox, so end-to-end coverage is not possible. Smoke "endpoint accepts a well-formed email and returns 200" could be added but offers little value; deferred.
- **`/users/logout` and `/users/delete-account` as dedicated specs** — covered implicitly by `authedRequest` fixture teardown; testing them in isolation demonstrates no technique not already in the backlog.
- **PUT-vs-PATCH decision table on `/notes/{id}`** — the API forces a clean split (PUT for full replace, PATCH for completion toggle only). No combinatorial space to enumerate.
